import React from 'react';

/**
 * Setup context used by the AuthProvider
 * which will provide this context to the App
 */
const AuthContext = React.createContext();

export default AuthContext;