// Not used at the moment...
const AuthReducer = (state, action) => {
  switch (action.type) {
    case 'A':
      return {
        ...state
      };
    default:
      return {
        ...state
      }
  }
};